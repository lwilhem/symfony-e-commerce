{% extends 'base.html.twig' %}

{% block title %}Hello ProductListController!{% endblock %}

{% block body %}
    <section class="latest-products">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="section-heading">
                        <h2>Our Products</h2>
                        <a href="{{ path('app_app') }}">Back Home <i class="fa fa-angle-right"></i></a>
                    </div>
                </div>
            {% for product in products %}
                <div class="col-md-4">
                    <div class="product-item">
                        <a href="#"><img src="{{ asset('')}}" alt="Twig && image = bof/20" ></a>
                        <div class="down-content">
                            <a href="{{ path('app_product_page', {id: product.id}) }}"><h4>{{ product.name }}</h4></a>
                            <h6>c koi symfony?</h6>
                            <p>Lorem ipsume dolor sit amet, adipisicing elite. Itaque, corporis nulla aspernatur.</p>
                            <span>${{ product.price }}</span>
                        </div>
                    </div>
                </div>
            {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}
