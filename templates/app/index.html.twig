{% extends 'base.html.twig' %}

{% block title %}E-Commerce Moyen{% endblock %}

{% block body %}
<div class="latest-products">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="section-heading">
                    <h2>Our Shops</h2>
                    <a href="{{ path('app_product_list') }}"> See All Products <i class="fa fa-angle-right"></i></a>
                </div>
            </div>
            {% for category in categories %}
            <div class="col-md-4">
                <div class="product-item">
                    <a href="#"><img src="{{ asset(picture)}}" alt="Twig && image = bof/20" ></a>
                    <div class="down-content">
                        <a href="{{ path('app_category_list', {category_id: category.id}) }}"><h4>{{ category.name }}</h4></a>
                        <h6>c koi symfony?</h6>
                        <p>Lorem ipsume dolor sit amet, adipisicing elite. Itaque, corporis nulla aspernatur.</p>
                        <ul class="stars">
                            <li><i class="fa fa-star"></i></li>
                            <li><i class="fa fa-star"></i></li>
                            <li><i class="fa fa-star"></i></li>
                            <li><i class="fa fa-star"></i></li>
                            <li><i class="fa fa-star"></i></li>
                        </ul>
                        <span>Reviews ({{ category.id }})</span>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
