{% extends 'base.html.twig' %}

{% block title %}Hello CategoryShopController!{% endblock %}

{% block body %}
    <section>
        {% for product in products %}
            <span class="text-black">{{ product.name }}</span>
        {% endfor %}
    </section>
{% endblock %}
